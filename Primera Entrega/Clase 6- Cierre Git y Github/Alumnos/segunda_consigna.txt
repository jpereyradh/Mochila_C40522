*configuración*
1) establecer un nombre de usuario
git config -- global user.name ""
2) establecer un correo electrónico
git config --global user.email ""

*iniciando un repositorio*
1) iniciar git en la carpeta donde está el proyecto
git init
2) clonar el repositorio de github
git clone <url>
3) agregar todos los archivos para el commit
git add .
3) hacer el primer commit
git commit -m ""
4) subir el repositorio
git push origin main
5) bajar información del repositorio
git pull origin main

*conectando con un repositorio remoto*
1) conectar con el repositorio
git remote add origin <url>
2) cambiar de remote
git remote set-url origin <url>
3) remover repositorio
git remote rm <name/origin>
4) mostrar la lista de repositorios
git remote remote -vn

*ramas*
1) crear una rama
git branch ""
2) mostrar la lista de ramas
git branch
3) cambiar de rama
git checkout ""
4) unir ramas
git merge ""

*otros*
1) ver estado
git status
2) eliminar un repositorio
git rm ""
